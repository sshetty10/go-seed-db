type Trainer {
    id: String!
    name: String!
    age: Int!
    city: String!
    licenseID: String!
    licenseState: String!
}

type Query {
    Trainers: [Trainer!]!
    TrainerByID(id: String!): Trainer!
}